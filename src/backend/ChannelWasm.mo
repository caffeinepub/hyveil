// This file is auto-overwritten at build time by generate_channel_wasm.py
// which compiles channel.mo → channel.wasm and embeds it as a blob here.
// The placeholder empty blob below is replaced with the real channel template WASM
// before main.mo is compiled, so partner canisters get the full template on deployment.
module { public let wasm : Blob = ""; };
